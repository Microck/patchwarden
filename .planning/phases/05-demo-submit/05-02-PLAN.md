---
phase: 05-demo-submit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - demo/samples/suspicious_mod_src/README.md
  - demo/samples/suspicious_mod_src/src/main/java/com/jarspect/demo/DemoMod.java
  - demo/build_sample.sh
  - scripts/demo_run.sh
autonomous: true

must_haves:
  truths:
    - "Repo contains a SAFE synthetic demo jar builder"
    - "Demo script runs upload+scan end-to-end and prints verdict"
  artifacts:
    - path: "demo/build_sample.sh"
      provides: "Builds the synthetic demo jar"
    - path: "scripts/demo_run.sh"
      provides: "End-to-end demo script"
  key_links:
    - from: "scripts/demo_run.sh"
      to: "/upload and /scan"
      via: "curl"
      pattern: "curl"
---

<objective>
Create a SAFE synthetic demo sample and an end-to-end demo script.

Purpose: Demonstrate detection reliably without downloading or distributing real malware.
Output: Java demo source + build script that produces a suspicious-but-benign jar + run script.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@/home/ubuntu/workspace/ai-dev-days-hackathon/plans/jarspect.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add synthetic suspicious-but-benign demo mod source + build script</name>
  <files>
demo/samples/suspicious_mod_src/README.md
demo/samples/suspicious_mod_src/src/main/java/com/jarspect/demo/DemoMod.java
demo/build_sample.sh
  </files>
  <action>
Create a tiny Java program packaged as a jar that is SAFE:
- it must not download anything, not write files, not execute processes
- it may contain strings and dead-code examples that LOOK suspicious to pattern matching (e.g., URL strings, base64-like literals, `Runtime.exec` in an uncalled method)

Add a build script `demo/build_sample.sh` that compiles and packages a jar under `demo/` (e.g., `demo/suspicious_sample.jar`).

Document explicitly in `demo/samples/suspicious_mod_src/README.md` that this is a synthetic fixture for demo/testing.
  </action>
  <verify>bash demo/build_sample.sh</verify>
  <done>Build script produces a jar artifact locally without fetching external content.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end demo runner script</name>
  <files>scripts/demo_run.sh</files>
  <action>
Create `scripts/demo_run.sh` that:
- assumes the API server is running
- builds the demo jar (calls `demo/build_sample.sh`)
- uploads the jar
- runs `/scan` with demo author metadata (new author, few/no prior mods, some reports)
- prints the verdict tier/score and top indicators in the terminal
  </action>
  <verify>bash scripts/demo_run.sh</verify>
  <done>Script performs upload+scan and prints a verdict consistently.</done>
</task>

</tasks>

<verification>
- Run server, then `bash scripts/demo_run.sh`
- UI and API both show a HIGH-ish verdict due to synthetic indicators
</verification>

<success_criteria>
- Demo can be reproduced without real malware samples
</success_criteria>

<output>
After completion, create `.planning/phases/05-demo-submit/05-02-SUMMARY.md`
</output>
